  
  
  Un arreglo es una estructura de dato que orgniza elementos
de forma lineal donde cada elemento es accedido a traves 
de un indice

permiten organizar mejor daos relacionados, facilita
el acceso y modificaion de los datos con los indices 
permite ajustar mas o menis elementos sin crear nuevas variables
ademas de permitit hacer operaciones en lote como filtrar u prdenar

el alamacenamientos de los datos de la lista tiene un orden secuencial
son mutables, indexables y ordenadas

lista_vacia = []
lista_vacia = list()

cuando creamos una lista se reserva espacio en memoria
donde se le asigna un tamaño y se le da una referencia 
para poder accederla asociandole un id 22554420 donde esa variable 
que contiene la refencia se asocia al espacio de memoria reservado 0x44EF
para la lista

dicha variable se maneja en el stack o pila de la memoria mientras
que el espacio reservado se maneja en el heap 
----------------------------------
Defincion de una lista:
nombres = ["Rosita","juan"]


inicializar lista :mis_lista = [0]*5

for i in range(len(mi_lista)):
    mi_lista [i] = i


-------------------------------------------------------
el primer elemento de una lista tiene indice 0
el ultimo elemento de la lista en python se puede acceder con -
-------------------------------------------------

las lista son dinamicas es decir que se pueden eliminar
o agregar elementos en ejecucion

--------------------------------------
con la funcion len(lista) puedp ver la cantidad de elemento de la lista




numeros = [1, 2, 3, 4, 5]

print(numeros)

lo ideal para imprimir los elementos de una slista seria asi: 

for i in range(len(numeros)):
    print(numeros[i])

--------------------------------------------

Existen dos formas de cargas una lista de forma:

*Secuencial: donde se carga desde el primer elemento
de la lista hasta el ultimo en forma sucesiva
*Aleatoria: donde se define manualmente la posicion y valor a cargar

------------------------

list_1 = list_2

la modificaciones que se sufran en lista 1
las sufrira la lista dos tambien pues esta copia es su id


------------------------------
siempre es recomendable verificar si type es de tipo listya
cuando se recibe como parametro
